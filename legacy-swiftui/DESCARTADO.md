# Descartado el 07/09/2026

El dueño decidió hacer la app con Expo (React Native) y EAS Build en vez de SwiftUI nativo,
para poder probar en Expo Go y compilar en la nube sin Mac. La app vigente está en `../app/`.
Estas fuentes SwiftUI nunca se compilaron. Se dejan como referencia del diseño de pantallas.
